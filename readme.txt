Unpack zip, open with IDea or just run with maven

Configured to run with Java11 but could be upgraded

to run from terminal - mvn clean test

Allure report is already integrated (will need plugin in Jenkins)

to run allure report locally execute:
mvn allure:serve


Important notes:
Add username and password to AccConsts class
